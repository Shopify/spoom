name: spoom
nix: true
type: ruby

up:
  - ruby
  - bundler
  - packages:
      - libyaml

commands:
  typecheck:
    aliases: [ "tc" ]
    run:     "bin/typecheck"
  console:   "bin/console"
  test:      "bin/test"
  style:     "bin/style"
  sanity:    "bin/typecheck && bin/style && bin/test"
