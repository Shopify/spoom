name: spoom

type: ruby

up:
  - ruby: 3.2.2
  - bundler

commands:
  typecheck:
    aliases: [ "tc" ]
    run:     "bin/typecheck"
  console:   "bin/console"
  test:      "bin/test"
  style:     "bin/style"
  sanity:    "bin/typecheck && bin/style && bin/test"
